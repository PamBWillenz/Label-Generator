<!-- filepath: /Users/pamwillenz/code/label_maker/app/views/lists/new.html.erb -->
<h1>New List</h1>

<%= simple_form_for @list do |form| %>
<% if @list.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

  <ul>
    <% @list.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="form-group">
  <%= form.input :name, input_html: { class: 'form-control' } %>
</div>

<div class="form-group">
  <%= form.input :description, input_html: { class: 'form-control' } %>
</div>

<div class="actions">
  <%= form.button :submit, class: 'btn btn-primary' %>
</div>
<% end %>

<%= link_to 'Back', lists_path, class: 'btn btn-secondary' %>